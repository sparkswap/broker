const createMarketOrder = require('./create-market-order')
const createGoodTilCancelledOrder = require('./create-good-til-cancelled-order')

module.exports = {
  createMarketOrder,
  createGoodTilCancelledOrder
}
