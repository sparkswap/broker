syntax = "proto3";

import "google/protobuf/empty.proto";

message NewAddressResponse {
  string address = 0;
}

service Wallet {
  rpc NewAddress (google.protobuf.Empty) returns (NewAddressResponse);
}
