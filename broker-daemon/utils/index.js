/**
 * @author kinesis
 */
const getRecords = require('./get-records')
const logger = require('./logger')
const loadProto = require('./load-proto')

module.exports = { getRecords, logger, loadProto }
