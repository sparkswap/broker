#!/usr/bin/env node

/**
 * Kinesis Broker CLI
 */

const program = require('caporal');

const { version: CLI_VERSION } = require('../package.json');
// Requires all commands for the broker-cli
require('../broker-cli');

program
  .version(CLI_VERSION)
  .description('KINESIS BROKER CLI');

program.parse(process.argv);
